Running The program
1. Open terminal or cmd and `cd` to the root of the project
2. Create a virtual environment (don't type the $ sign)
  $ python -m venv env
  $ pip install -r requirements.txt
3. Activate the virtual environment
  $ source env/bin/activate // in linux terminal or _> env\Scripts\activate in windows
4. Run the program
  $ python main.py